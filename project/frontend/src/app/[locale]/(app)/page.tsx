/**
 * Homepage - Conversational Storefront (Primary Interface)
 *
 * ChatArea now lives permanently in StorefrontLayout (always mounted).
 * This page renders null — the layout handles showing/hiding ChatArea via CSS visibility.
 */
export default function HomePage() {
  return null
}
