# Crawl4AI - Lightweight web crawler with JavaScript rendering
# Version ~0.7.x for compatibility
crawl4ai>=0.7.0,<0.8.0

# Web framework for API endpoint (optional)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# Async HTTP client
httpx>=0.27.0
aiohttp>=3.9.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.1.0

# Playwright for browser automation (headless Chrome)
playwright>=1.41.0

# Environment variables
python-dotenv>=1.0.0

# IMPORTANT: NO ML/AI DEPENDENCIES
# Intentionally excluding:
# - sentence-transformers (large dependency, includes torch)
# - torch (large dependency, not needed for basic crawling)
# - transformers (not needed for basic crawling)
#
# This keeps the Docker image lightweight (<1GB vs >5GB with ML deps)
